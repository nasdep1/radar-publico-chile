[pytest]
# Solo tests unitarios: evita recolectar scripts/test_mercado_publico_api.py,
# que consulta la API real.
testpaths = tests
# Permite importar el paquete `src` desde la raíz del proyecto.
pythonpath = .
